"""Lightweight conversational operation helpers.

These implementations provide a minimal subset of the qtpyvcp conversatinal
helpers used by the widgets in this repository. They are intentionally simple
and avoid external dependencies so the widgets can run even when the optional
qtpyvcp conversational package is not installed.
"""

